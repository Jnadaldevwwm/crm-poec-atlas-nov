import { RoleTransformPipe } from './role-transform.pipe';

describe('RoleTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
